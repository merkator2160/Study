<Window x:Class="GridViewSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GridViewSample"
        Title="ListView Sample" Height="350" Width="525">
    <Grid>
        
        <Grid.Resources>
            <local:PersonList x:Key="PersonList"></local:PersonList>
        </Grid.Resources>
        
        <ListView Name="listView1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" 
                HorizontalContentAlignment="Stretch"
                ItemsSource="{StaticResource PersonList}" 
                MouseDoubleClick="ListView_MouseDoubleClick">
            
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=FirstName}" Header="First Name" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=LastName}" Header="Last Name" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Age}" Header="Age" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Position}" Header="Position" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
