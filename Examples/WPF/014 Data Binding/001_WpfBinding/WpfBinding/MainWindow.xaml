<Window x:Class="WpfBinding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Простая привязка данных" Height="350" Width="525">
    <StackPanel Margin="12">
        
        <!--Элемент источник данных-->
        <Slider Name="silderFontSize" 
                Minimum="12" 
                Maximum="80" 
                Margin="10"></Slider>

        <!--Элемент с целевым свойством привязки-->
        <TextBlock Margin="10" 
                   FontSize="{Binding ElementName=silderFontSize, Path=Value}">
            Hello world
        </TextBlock>
    </StackPanel>
</Window>
