<Window x:Class="SetNonDependencyProp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка привязки для обычного свойства" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Margin="5">Содержимое второго параграфа:</TextBlock>
        <!--Свойство Text у объекта Run обычное свойство - к нему нельзя применить привязку.-->
        <TextBox Margin="5" Grid.Row="1"  Name="tb1"
                 MinLines="2" 
                 TextWrapping="Wrap" 
                 AcceptsReturn="True"
                 Text="{Binding ElementName=runParagraphTwo, Path=Text, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}">
        </TextBox>

        <FlowDocumentScrollViewer Grid.Row="2">
            <FlowDocument>
                <Paragraph>Это параграф 1.</Paragraph>
                <Paragraph>
                    <Run Name="runParagraphTwo" ></Run>
                </Paragraph>
            </FlowDocument>
        </FlowDocumentScrollViewer>
    </Grid>
</Window>
