<Window x:Class="DataContextSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DataContextSample"
        Title="Использование DataContext" Height="350" Width="525">
    
    <Window.Resources>
        <local:Person x:Key="PersonDataSource" 
                      FirstName="Jhon"
                      LastName="Doe">
        </local:Person>
    </Window.Resources>
    
    <Grid>
        <!--Установив DataContext, вложенные элементы могут использовать Path без указания источника данных.-->
        <StackPanel Margin="10" Name="panel1"
                    DataContext="{StaticResource PersonDataSource}">
            
            <TextBlock>First name</TextBlock>
            <TextBox Text="{Binding FirstName}"></TextBox>

            <TextBlock></TextBlock>
            
            <TextBlock>Last name</TextBlock>
            <TextBox Text="{Binding LastName}"></TextBox>
            
        </StackPanel>
    </Grid>
</Window>
